extends: "@ciscospark/eslint-config/mocha"
rules:
  no-invalid-this:
    - 0
  # we want to use the older technique in the tests for now to give us more
  # confidence that we're using the new one correctly
  no-restricted-properties: off
